# React Application Environment Variables Example
# Copy this file to .env and replace values as needed

# Image Processor service endpoint (required for image processing functionality)
REACT_APP_IMAGE_PROCESSOR_URL=http://localhost:3002

# Picture Server endpoint (for direct testing - usually accessed through image processor)
REACT_APP_API_URL=http://localhost:3001

# Application environment
REACT_APP_ENV=development
REACT_APP_VERSION=1.0.0

# Observability & Telemetry (Grafana Faro) - Optional
# Uncomment and configure these for production observability
# REACT_APP_FARO_URL=https://faro.grafana.net/api/collect/v1
# REACT_APP_FARO_API_KEY=your_faro_api_key_here

# Source map generation for debugging
GENERATE_SOURCEMAP=true

# Development settings (not recommended for production)
NODE_TLS_REJECT_UNAUTHORIZED=0

# --- Production Examples ---
# REACT_APP_IMAGE_PROCESSOR_URL=https://your-image-processor-domain.com
# REACT_APP_API_URL=https://your-api-gateway-url.execute-api.region.amazonaws.com/Prod
# REACT_APP_ENV=production
# GENERATE_SOURCEMAP=false
# Remove or set NODE_TLS_REJECT_UNAUTHORIZED=1 for production
